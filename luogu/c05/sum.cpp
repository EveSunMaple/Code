#include <algorithm>
#include <iostream>
#include <cstring>
#include <cstdio>
#define fastio                   \
    ios::sync_with_stdio(false); \
    cin.tie(0);                  \
    cout.tie(0)

using namespace std;

int main()
{
    fastio;
    //freopen("sum.in", "r", stdin);
    //freopen("sum.out", "w", stdout);
    int T;
    scanf("%d", &T);
    while (T--)
    {
        
    }
    return 0;
}